pipeline {
    agent {
        label 'docker' 
    }
    stages {
        stage('build') {
            agent {
                docker {
                    label 'docker'
                    image 'kivy/buildozer:latest'
                    args '-v ' + env.WORKSPACE + ':/home/user/hostcwd --entrypoint=""'
                }
            }
            steps {
                sh 'buildozer --version'
                sh '[[ ! -f android-ndk-r23c-linux.zip ]] && wget https://dl.google.com/android/repository/android-ndk-r23c-linux.zip'
                sh 'yes | buildozer android debug'
            }
        }
    }
}