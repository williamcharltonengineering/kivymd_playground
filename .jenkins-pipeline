pipeline {
    agent {
        label 'docker' 
    }
    stages {
        stage('build') {
            agent {
                docker {
                    label 'docker'
                    image 'kivy/buildozer:latest'
                    args '-v "$(pwd)":/home/user/hostcwd'
                }
            }
            steps {
                steps {
                    sh 'buildozer --version'
                }
            }
        }
    }
}