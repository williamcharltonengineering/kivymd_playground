pipeline {
    agent {
        label 'docker' 
    }
    stages {
        stage('build') {
            agent {
                docker {
                    label 'docker'
                    image 'kivy/buildozer:latest'
                    args '-v ' + env.WORKSPACE + ':/home/user/hostcwd --entrypoint=""'
                }
            }
            steps {
                sh 'buildozer --version'
                sh 'if [ ! -f android-ndk-r23c-linux.zip ] ; then curl https://dl.google.com/android/repository/android-ndk-r23c-linux.zip -o android-ndk-r23c-linux.zip ; fi'
                sh 'yes | buildozer android debug'
            }
        }
    }
}